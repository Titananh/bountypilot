# Local Lab Authorization

This example program is limited to assets owned by the researcher on the local machine:

- `localhost`
- `127.0.0.1`

It is not authorization to test public third-party systems. Use `lab-offensive` only for local labs, CTFs, intentionally vulnerable apps, or assets you fully own.
