# VM Bootstrap

Generate install guidance without executing installers:

```bash
bounty arsenal vm --write
bounty arsenal bootstrap --level safe --write
bounty tools doctor --json
```

Review generated scripts before running them manually inside your VM.

